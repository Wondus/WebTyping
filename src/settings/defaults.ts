import type { Settings } from './settings-types';
export const DEFAULT_SETTINGS: Settings = { version: 6, segmentWords: 10, idleSeconds: 3, highlightedNextWords: 0, caseSensitive: true, skipEmojis: true, skipPunctuation: false, fontSize: 26, columnWidth: 840 };
